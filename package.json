{
  "name": "turbi",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "airports": "node scripts/build-airports.mjs",
    "serve": "python3 -m http.server 8000",
    "start": "node --max-old-space-size=256 --max-semi-space-size=8 server/live.mjs"
  },
  "devDependencies": {
    "geo-tz": "^8.1.9",
    "jsdom": "^29.1.1",
    "vitest": "^3.2.0"
  },
  "engines": {
    "node": ">=22"
  }
}
